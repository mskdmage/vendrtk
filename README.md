# vendrtk

A Rust vendor reconciliation toolkit for parsing vendor invoices and Statements of Work (SOWs) into structured output.

Upload a PDF, run it through OCR and extraction, get back clean CSV records.